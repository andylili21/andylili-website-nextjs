# 项目架构可视化

## 📂 完整目录结构

```
andylili-website-nextjs/
│
├── 📱 前端 (Next.js)
│   ├── app/                          # Next.js App Router
│   │   ├── [locale]/                # 国际化路由
│   │   │   ├── blog/                # 博客页面
│   │   │   ├── projects/            # 项目展示页面
│   │   │   └── layout.jsx           # 布局组件
│   │   ├── lib/                     # 工具库
│   │   │   └── api/                 # ✨ API 客户端（新增）
│   │   │       ├── client.js        # 基础请求封装
│   │   │       ├── posts.js         # 文章 API
│   │   │       └── projects.js      # 项目 API
│   │   └── globals.css              # 全局样式
│   │
│   ├── components/                   # React 组件
│   │   ├── ui/                      # UI 组件库
│   │   ├── Header.jsx               # 头部组件
│   │   ├── Footer.jsx               # 底部组件
│   │   └── ThemeToggle.jsx          # 主题切换
│   │
│   ├── public/                       # 静态资源
│   │   ├── images/                  # 图片
│   │   └── icons/                   # 图标
│   │
│   └── .env.local                    # ✨ 前端环境变量（新增）
│
├── 🔧 后端 (Express)
│   └── server/
│       ├── src/                      # ✨ 新架构（全新）
│       │   ├── 📦 config/           # 配置层
│       │   │   ├── database.js      # Prisma 客户端单例
│       │   │   └── server.js        # 服务器配置常量
│       │   │
│       │   ├── 🎮 controllers/      # 控制器层（HTTP 请求处理）
│       │   │   ├── postController.js      # 文章控制器
│       │   │   └── projectController.js   # 项目控制器
│       │   │
│       │   ├── 💼 services/         # 服务层（业务逻辑）
│       │   │   ├── postService.js         # 文章服务
│       │   │   └── projectService.js      # 项目服务
│       │   │
│       │   ├── 🛡️ middlewares/      # 中间件层
│       │   │   ├── errorHandler.js        # 统一错误处理
│       │   │   ├── validator.js           # 参数验证（express-validator）
│       │   │   └── logger.js              # 请求日志（morgan）
│       │   │
│       │   ├── 🧰 utils/            # 工具层
│       │   │   ├── response.js            # 统一响应格式
│       │   │   └── helpers.js             # 辅助函数
│       │   │
│       │   ├── 🛣️ routes/           # 路由层
│       │   │   ├── posts.js               # 文章路由
│       │   │   └── projects.js            # 项目路由
│       │   │
│       │   └── app.js                # ✨ 应用入口（新版）
│       │
│       ├── routes/ (旧)              # ⚠️ 将被废弃
│       │   ├── posts.js              # 原始文章路由
│       │   └── projects.js           # 原始项目路由
│       │
│       ├── index.js (旧)             # ⚠️ 将被废弃（使用 src/app.js）
│       ├── .env                      # ✨ 后端环境变量（新增）
│       └── .env.example              # ✨ 环境变量示例（新增）
│
├── 🗄️ 数据库 (Prisma + SQLite)
│   └── prisma/
│       ├── schema.prisma             # 数据库模型定义
│       │                             # - Post (文章)
│       │                             # - Project (项目)
│       │                             # - Category (分类)
│       │
│       ├── seed.js                   # ✨ 种子数据（新增）
│       │                             # - 3 个分类
│       │                             # - 4 篇测试文章
│       │                             # - 3 个测试项目
│       │
│       ├── migrations/               # 数据库迁移文件
│       └── dev.db                    # SQLite 数据库文件（运行后生成）
│
├── 📚 文档
│   ├── DEVELOPMENT_README.md         # ✨ 完整开发文档（新增，443行）
│   ├── QUICK_START.md                # ✨ 快速启动指南（新增，184行）
│   ├── INITIALIZATION_SUMMARY.md     # ✨ 初始化总结（新增，345行）
│   ├── PROJECT_ARCHITECTURE.md       # ✨ 本文档（新增）
│   └── README.md                     # 原始 README
│
├── 🔧 配置文件
│   ├── package.json                  # ✨ 已更新（添加开发脚本）
│   ├── .env.local                    # ✨ 前端环境变量（新增）
│   ├── .env.local.example            # ✨ 前端环境变量示例（新增）
│   ├── tsconfig.json                 # TypeScript 配置
│   ├── next.config.ts                # Next.js 配置
│   ├── tailwind.config.js            # TailwindCSS 配置
│   └── eslint.config.mjs             # ESLint 配置
│
└── 🛠️ 工具脚本
    └── verify-setup.sh               # ✨ 项目验证脚本（新增）
```

## 🏗️ 后端分层架构详解

```
HTTP 请求流程：

┌─────────────────────────────────────────────────────────────────┐
│                         客户端请求                                │
│                    (浏览器/Postman/前端)                          │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Express 中间件栈                             │
├─────────────────────────────────────────────────────────────────┤
│  1. helmet()              - 安全 HTTP 头                         │
│  2. cors()                - 跨域配置                             │
│  3. express.json()        - JSON 解析                            │
│  4. requestLogger         - 请求日志（morgan）                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        路由层 (Routes)                            │
├─────────────────────────────────────────────────────────────────┤
│  📄 routes/posts.js       - 定义文章相关路由                      │
│  📄 routes/projects.js    - 定义项目相关路由                      │
│                                                                   │
│  功能：                                                           │
│  • 路由匹配 (GET /api/posts)                                     │
│  • 参数验证 (postValidationRules.list)                          │
│  • 调用控制器                                                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      控制器层 (Controllers)                       │
├─────────────────────────────────────────────────────────────────┤
│  📄 postController.js     - 处理文章请求                         │
│  📄 projectController.js  - 处理项目请求                         │
│                                                                   │
│  职责：                                                           │
│  • 解析 HTTP 请求参数 (req.query, req.body)                      │
│  • 调用 Service 层方法                                           │
│  • 构造 HTTP 响应 (successResponse/errorResponse)               │
│  • 处理异常 (next(error))                                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                       服务层 (Services)                           │
├─────────────────────────────────────────────────────────────────┤
│  📄 postService.js        - 文章业务逻辑                         │
│  📄 projectService.js     - 项目业务逻辑                         │
│                                                                   │
│  职责：                                                           │
│  • 实现业务逻辑（slug生成、阅读时长计算）                          │
│  • 数据验证和转换                                                 │
│  • 调用 Prisma 进行数据库操作                                     │
│  • 抛出业务异常                                                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    数据访问层 (Prisma ORM)                        │
├─────────────────────────────────────────────────────────────────┤
│  📄 config/database.js    - Prisma 客户端单例                    │
│                                                                   │
│  操作：                                                           │
│  • prisma.post.findMany()    - 查询多条记录                     │
│  • prisma.post.findUnique()  - 查询单条记录                     │
│  • prisma.post.create()      - 创建记录                         │
│  • prisma.post.update()      - 更新记录                         │
│  • prisma.post.delete()      - 删除记录                         │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SQLite 数据库                                │
│                     (prisma/dev.db)                               │
└─────────────────────────────────────────────────────────────────┘

如果发生错误，在任何层抛出异常 ─┐
                                 │
                                 ▼
                    ┌────────────────────────────┐
                    │  错误处理中间件              │
                    │  (errorHandler)             │
                    ├────────────────────────────┤
                    │ • 捕获所有异常              │
                    │ • Prisma 错误转换          │
                    │ • 返回统一错误格式          │
                    └────────────────────────────┘
```

## 🔄 API 请求示例流程

### 示例：获取文章列表

```javascript
// 1. 客户端发起请求
fetch('http://localhost:3001/api/posts?page=1&limit=10&status=PUBLISHED')

// 2. 路由匹配 (routes/posts.js)
router.get('/', postValidationRules.list, postController.getAllPosts);

// 3. 参数验证 (middlewares/validator.js)
query('page').optional().isInt({ min: 1 })  // 验证通过

// 4. 控制器处理 (controllers/postController.js)
async getAllPosts(req, res, next) {
  const result = await postService.getAllPosts(req.query);
  res.json(successResponse(result, result.pagination));
}

// 5. 服务层执行 (services/postService.js)
async getAllPosts(filters) {
  const where = { status: 'PUBLISHED' };
  const posts = await prisma.post.findMany({ where, skip: 0, take: 10 });
  const total = await prisma.post.count({ where });
  return { posts, pagination: {...} };
}

// 6. 数据库查询 (Prisma)
SELECT * FROM Post WHERE status = 'PUBLISHED' LIMIT 10 OFFSET 0;
SELECT COUNT(*) FROM Post WHERE status = 'PUBLISHED';

// 7. 返回响应
{
  "success": true,
  "data": {
    "posts": [...],
    "pagination": { "page": 1, "limit": 10, "total": 15, "totalPages": 2 }
  }
}
```

## 📊 数据模型关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                          Category                                │
│  (分类)                                                           │
├─────────────────────────────────────────────────────────────────┤
│  • id: Int (主键)                                                │
│  • name: String (唯一)                                           │
│  • slug: String (唯一)                                           │
│  • createdAt: DateTime                                           │
│  • updatedAt: DateTime                                           │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ 一对多关系 (一个分类有多篇文章)
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                            Post                                  │
│  (文章)                                                           │
├─────────────────────────────────────────────────────────────────┤
│  • id: Int (主键)                                                │
│  • slug: String (唯一, 索引)                    ─────────────┐  │
│  • title: String                                              │  │
│  • excerpt: String                                            │  │
│  • content: String                                            │  │
│  • coverImage: String?                                        │  │
│  • publishedAt: DateTime? (索引)                              │  │
│  • readingTime: Int?                                          │  │
│  • featured: Boolean (索引)                                   │  │
│  • viewCount: Int                                             │  │
│  • status: PostStatus (枚举, 索引)                            │  │
│  • tags: String (逗号分隔: "Next.js,React")                   │  │
│  • categoryId: Int? (外键 → Category.id)                      │  │
│  • createdAt: DateTime                                        │  │
│  • updatedAt: DateTime                                        │  │
└───────────────────────────────────────────────────────────────┘  │
                                                                    │
     用于 URL 访问: /blog/getting-started-with-nextjs ─────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                          Project                                 │
│  (项目)                                                           │
├─────────────────────────────────────────────────────────────────┤
│  • id: Int (主键)                                                │
│  • title: String                                                 │
│  • description: String                                           │
│  • technologies: String (逗号分隔: "React,Node.js")               │
│  • coverImage: String?                                           │
│  • githubUrl: String?                                            │
│  • demoUrl: String?                                              │
│  • featured: Boolean (索引)                                      │
│  • order: Int (索引, 用于排序)                                    │
│  • status: ProjectStatus (枚举, 索引)                            │
│  • createdAt: DateTime                                           │
│  • updatedAt: DateTime                                           │
└─────────────────────────────────────────────────────────────────┘

枚举类型：
  PostStatus: DRAFT | PUBLISHED
  ProjectStatus: ACTIVE | ARCHIVED
```

## 🎨 前端 API 调用流程

```
┌─────────────────────────────────────────────────────────────────┐
│               Next.js Server Component                           │
│               (app/blog/page.js)                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ import { getAllPosts } from '@/app/lib/api/posts'
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  app/lib/api/posts.js                            │
│                  (文章 API 封装)                                  │
├─────────────────────────────────────────────────────────────────┤
│  export async function getAllPosts(params) {                    │
│    return get('/api/posts', params);                            │
│  }                                                               │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ 调用基础请求方法
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  app/lib/api/client.js                           │
│                  (基础请求封装)                                   │
├─────────────────────────────────────────────────────────────────┤
│  • 拼接 API_BASE_URL (http://localhost:3001)                    │
│  • 添加请求头 (Content-Type: application/json)                  │
│  • 发起 fetch 请求                                               │
│  • 解析 JSON 响应                                                │
│  • 检查 response.ok 和 data.success                             │
│  • 错误处理（抛出 APIError）                                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ HTTP Request
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│              后端 API (Express Server)                           │
│              http://localhost:3001/api/posts                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ JSON Response
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  返回数据格式：                                                   │
│  {                                                               │
│    "success": true,                                             │
│    "data": {                                                    │
│      "posts": [...],                                            │
│      "pagination": {...}                                        │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 安全防护层

```
┌────────────────────────────────────────────────────────────────┐
│                        请求进入                                  │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  1. Helmet 中间件                                               │
│     • 设置安全 HTTP 响应头                                      │
│     • 防止 XSS、点击劫持等攻击                                   │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  2. CORS 中间件                                                 │
│     • 检查请求来源                                              │
│     • 仅允许 ALLOWED_ORIGINS 中的域名                           │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  3. express.json() 限制                                         │
│     • 限制请求体大小 (10mb)                                     │
│     • 防止过大请求攻击                                          │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  4. express-validator                                           │
│     • 验证参数类型、长度、格式                                   │
│     • 防止 SQL 注入、XSS                                        │
│     • 返回详细错误信息                                          │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  5. 业务逻辑层                                                  │
│     • Service 层二次验证                                        │
│     • 数据清理和转换                                            │
└────────────────┬───────────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│  6. Prisma ORM                                                  │
│     • 参数化查询（防 SQL 注入）                                  │
│     • 类型安全                                                  │
└────────────────────────────────────────────────────────────────┘
```

## 📝 核心文件说明

### 后端核心文件（14个）

| 文件路径 | 职责 | 代码行数 |
|---------|------|---------|
| `server/src/app.js` | Express 应用入口，中间件配置 | 67 |
| `server/src/config/database.js` | Prisma 客户端单例 | 19 |
| `server/src/config/server.js` | 服务器配置常量 | 9 |
| `server/src/utils/response.js` | 统一响应格式工具 | 63 |
| `server/src/utils/helpers.js` | 辅助函数（slug、阅读时长） | 55 |
| `server/src/middlewares/errorHandler.js` | 错误处理中间件 | 60 |
| `server/src/middlewares/validator.js` | 参数验证规则 | 170 |
| `server/src/middlewares/logger.js` | 请求日志中间件 | 17 |
| `server/src/services/postService.js` | 文章业务逻辑 | 193 |
| `server/src/services/projectService.js` | 项目业务逻辑 | 150 |
| `server/src/controllers/postController.js` | 文章控制器 | 94 |
| `server/src/controllers/projectController.js` | 项目控制器 | 70 |
| `server/src/routes/posts.js` | 文章路由定义 | 29 |
| `server/src/routes/projects.js` | 项目路由定义 | 23 |

### 前端核心文件（3个）

| 文件路径 | 职责 | 代码行数 |
|---------|------|---------|
| `app/lib/api/client.js` | API 基础请求封装 | 121 |
| `app/lib/api/posts.js` | 文章 API 方法 | 74 |
| `app/lib/api/projects.js` | 项目 API 方法 | 56 |

### 数据库文件（1个）

| 文件路径 | 职责 | 代码行数 |
|---------|------|---------|
| `prisma/seed.js` | 种子数据脚本 | 262 |

### 文档文件（4个）

| 文件路径 | 内容 | 字数 |
|---------|------|------|
| `DEVELOPMENT_README.md` | 完整开发文档 | ~8000字 |
| `QUICK_START.md` | 快速启动指南 | ~3000字 |
| `INITIALIZATION_SUMMARY.md` | 初始化总结 | ~6000字 |
| `PROJECT_ARCHITECTURE.md` | 架构说明（本文档） | ~4000字 |

## 🎯 关键设计决策

### 1. 为什么选择分层架构？

- **职责分离**: 每层有明确职责，易于理解和维护
- **代码复用**: Service 层可被多个 Controller 复用
- **易于测试**: 每层可独立单元测试
- **易于扩展**: 添加新功能只需在对应层添加代码

### 2. 为什么使用统一响应格式？

- **前端处理简单**: 统一的数据结构
- **错误信息清晰**: 包含错误代码、消息和详情
- **易于调试**: 标准格式便于日志分析

### 3. 为什么前后端分离？

- **独立部署**: 前后端可分别部署和扩展
- **技术栈灵活**: 可以独立升级技术栈
- **团队协作**: 前后端团队可并行开发
- **学习价值**: 了解真实项目架构

### 4. 为什么选择 Prisma？

- **类型安全**: TypeScript 类型定义
- **迁移管理**: 自动生成迁移文件
- **查询简洁**: 比 SQL 更易读易写
- **数据库无关**: 支持 SQLite、PostgreSQL、MySQL 等

## 🚀 总结

本项目通过标准化的分层架构、完善的错误处理、安全的 API 设计和详细的文档，为全栈开发提供了最佳实践示例。

**核心价值**:
- ✅ 生产级代码质量
- ✅ 完整的开发文档
- ✅ 便捷的开发体验
- ✅ 扎实的学习价值

---

📖 **相关文档**:
- [QUICK_START.md](./QUICK_START.md) - 快速启动
- [DEVELOPMENT_README.md](./DEVELOPMENT_README.md) - 详细开发文档
- [INITIALIZATION_SUMMARY.md](./INITIALIZATION_SUMMARY.md) - 初始化总结
